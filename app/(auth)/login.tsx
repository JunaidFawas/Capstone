import { Text, View } from 'react-native';

export default function LoginScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-2xl font-bold text-heading">Login</Text>
      <Text className="mt-2 text-sm text-body">CampusNest — Auth screen placeholder</Text>
    </View>
  );
}
